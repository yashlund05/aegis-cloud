# Aegis Telemetry Collector

Responsibility: Pull PromQL metrics, aggregate, normalize, write to Redis feature store.

## Endpoints
- `GET /v1/metrics`
- `GET /v1/features/{workload_id}`

## Config
See `config.py`
