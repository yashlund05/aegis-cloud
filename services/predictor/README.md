# Aegis Predictor

Responsibility: Per-workload quantile forecasts, model registry, drift detection.

## Endpoints
- `POST /v1/predict`
- `GET /v1/predictions`
- `GET /v1/models`
- `POST /v1/models/retrain`

## Config
See `config.py`
