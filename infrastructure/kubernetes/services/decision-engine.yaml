apiVersion: apps/v1
kind: Deployment
metadata:
  name: aegis-decision-engine
  namespace: aegis-system
  labels:
    app.kubernetes.io/name: decision-engine
    app.kubernetes.io/part-of: aegis
    app: aegis
    component: decision-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: decision-engine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: decision-engine
        app: aegis
        component: decision-engine
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
    spec:
      containers:
      - name: decision-engine
        image: aegis/decision-engine:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: aegis-decision-engine-config
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: aegis-decision-engine
  namespace: aegis-system
  labels:
    app.kubernetes.io/name: decision-engine
    app.kubernetes.io/part-of: aegis
spec:
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app.kubernetes.io/name: decision-engine
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: aegis-decision-engine-config
  namespace: aegis-system
data:
  PORT: "8000"
  REDIS_URL: "redis://redis.aegis-data:6379/0"
  DATABASE_URL: "postgres://aegis:changeme@postgres.aegis-data:5432/aegis_db"
